export enum OrderType {
    Normal = 'Normal',
    VIP = 'VIP'
}

export enum OrderStatus {
    Pending = 'Pending',
    Complete = 'Complete',
    Failed = 'Failed'
}
export enum BotStatus {
    Idle = 'Idle',
    Working = 'Working'
}
